% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_data_processing.R
\name{annotate_results_with_omim_go}
\alias{annotate_results_with_omim_go}
\title{Annotate gene-level results with OMIM phenotypes and GO terms}
\usage{
annotate_results_with_omim_go(
  results,
  add_omim = TRUE,
  omim_file = system.file("extdata", "omim-phenotype.txt", package = "rnawebapp"),
  add_go = TRUE,
  go_source = "ensembl"
)
}
\arguments{
\item{results}{A data.frame containing gene-level results with a column named `geneID`
corresponding to HGNC gene symbols (e.g., as used in OUTRIDER or FRASER).}

\item{add_omim}{Logical. Whether to annotate genes with OMIM phenotype data. Default is TRUE.}

\item{omim_file}{Path to the OMIM phenotype annotation file (TSV format). Default is `"./data/omim-phenotype.txt"`.
The file must include at least the columns: `OMIM` and `Phenotypes`.}

\item{add_go}{Logical. Whether to annotate genes with Gene Ontology (GO) terms. Default is TRUE.}

\item{go_source}{The BioMart source for retrieving GO terms. Default is `"ensembl"`.}
}
\value{
A data.frame with the original results, plus additional columns:
        - `Phenotypes`: OMIM phenotype(s) associated with each gene (if `add_omim = TRUE`)
        - `GO_TERMS`: GO term descriptions associated with each gene (if `add_go = TRUE`)
}
\description{
This function takes a data frame of gene-level results (e.g., from OUTRIDER or FRASER)
and optionally adds OMIM phenotype annotations and Gene Ontology (GO) term annotations.
It uses `org.Hs.eg.db` for OMIM links and `biomaRt` to retrieve GO terms via Ensembl.
}
