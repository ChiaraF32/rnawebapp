% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_data_processing.R
\name{merge_outrider_fraser}
\alias{merge_outrider_fraser}
\title{Merge OUTRIDER and FRASER Results by Gene and Sample}
\usage{
merge_outrider_fraser(ores, frares)
}
\arguments{
\item{ores}{A data frame of OUTRIDER results with `geneID` and `sampleID` columns.}

\item{frares}{A data frame of FRASER results with `geneID` and `sampleID` columns.}
}
\value{
A data frame with columns: `geneID`, `sampleID`, and `n_merged` (number of merged entries).
}
\description{
This function merges two data frames (`ores` and `frares`) containing OUTRIDER and FRASER results,
respectively. It merges them on `geneID` and `sampleID`, identifies duplicates, and returns a
collapsed data frame with a count of how many rows were merged per `(geneID, sampleID)` pair.
}
\examples{
merge_outrider_fraser(ores_df, frares_df)
}
